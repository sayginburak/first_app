<%= form_for :post, url: posts_path, method: :post do |p| %>
	<%= p.label :title %>
	<%= p.text_field :title %>
	<%= p.label :content %>
	<%= p.text_area :content %>
	<%= p.submit("Gönder") %>

<% end %>